apiVersion: v1
kind: Service
metadata:
  labels:
    app: ecgcdemo
  name: ecgcdemo
  namespace: default
 spec:
   ports:
   - port: 80
   - protocol: TCP
     targetPort: 80
   selector:
     app: ecgcdemo
   type: NodePort
