apiVersion: v1
kind: Service
metadata:
  labels:
    app: ecgc_demo
  name: ecgc_demo
  namespace: default
 spec:
   ports:
   - port: 80
   - protocol: TCP
     targetPort: 80
   selector:
     app: ecgc_demo
   type: NodePort
